# Skip peer dependency checks to avoid conflicts
legacy-peer-deps=true

# Force npm to include optional dependencies
include=optional

# Disable fund messages during install
fund=false

# Ensure package-lock.json is not used during builds
package-lock=false

# Force npm to rebuild native modules
rebuild=true

# Use npm cache more aggressively
cache-max=0